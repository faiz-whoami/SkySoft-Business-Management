@{
    ViewBag.Title = ViewBag.Title ?? "Coming soon";
}
<div class="empty-state">
    <i class="bi bi-hourglass-split"></i>
    <h2>@(ViewBag.ModuleName ?? ViewBag.Title)</h2>
    <p>This module is next. The page is wired up so you can already use the menu.</p>
</div>
